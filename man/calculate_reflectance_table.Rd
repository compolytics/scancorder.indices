% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_reflectance_table.R
\name{generate_reflectance_table}
\alias{generate_reflectance_table}
\title{Generate reflectance table from calibrated reflectance data}
\usage{
generate_reflectance_table(wavelengths, reflectance_list, meta_table = NULL)
}
\arguments{
\item{wavelengths}{Numeric vector of wavelengths (same length as each reflectance vector).}

\item{reflectance_list}{List of numeric reflectance vectors (calibrated or raw).}

\item{meta_table}{Optional data frame containing metadata for each sample.}
}
\value{
A data.frame with columns:
  - \code{sample}: integer sample number (1 to length of \code{reflectance_list})
  - one column per wavelength (named by wavelength, e.g. \code{R550}, \code{R700})
  - metadata columns (if meta_table is provided)
}
\description{
This function creates a data frame containing calibrated reflectance values
for each sample and wavelength. Each wavelength becomes a column in the output.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_indices.R
\name{calculate_indices}
\alias{calculate_indices}
\title{Calculate all available spectral indices for a list of reflectance vectors}
\usage{
calculate_indices(wavelengths, reflectance_list)
}
\arguments{
\item{wavelengths}{Numeric vector of wavelengths (same length as each reflectance vector).}

\item{reflectance_list}{List of numeric reflectance vectors.}
}
\value{
A data.frame with columns:
\itemize{
\item \code{sample}: integer sample number (1 to length of \code{reflectance_list})
\item one column per index (named by the index, e.g. \code{NDVI}, \code{NDWI})
}
}
\description{
This function discovers all index XML definitions in the
\verb{inst/indices/} folder of the \code{scancorder.indices} package, then
uses \code{calculate_index()} to compute each index for each reflectance
vector in \code{reflectance_list}.
}
